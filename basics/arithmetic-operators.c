#include <stdio.h>

int main() {
        //in C, logical operators is also the same as for many languages

        // + (addition) "don't forget that this symbol can also means concatanate"
        // - (subtraction)
        // * (multiplication)
        // '/' (division)
        // % (module "rest in divisions")
        // ++ (increment "summing one in value")
        // -- (decrement "subtracting one in value")

}