#include <stdio.h>

int main() {
    //in C, logical operators is the same as for many languages, but let's see each one anyway

    // && (and), sends "true" when every sentence is true
    // || (or), sends "true" when any sentence is true
    // ! (no), sends inverse value

}