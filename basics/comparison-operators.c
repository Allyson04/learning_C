#include <stdio.h>

int main() {
    //in C, comparison operators is the same as for many languages, but let's see each one anyway

    // < "less than"
    // > "more than"
    // <= "less or equal as"
    // >= "more or equal as"
    // == "equal as"
    // != "different than"

}